# Clean data


This directory contains clean sequencing data (FASTQ files)
after preprocessing and trimming module.

Each sample consists of paired-end reads, R1 and R2.

The files stored here are not versioned.