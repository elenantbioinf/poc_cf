# Test


This directory contains data used for testing and validation of the project.

It contains two subdirectories: one containing an original VCF file and another
containing a synthetic VCF file, wich is used to validate the pipeline.

These files are versioned in order to ensure reproducibility and
validation of the workflow.