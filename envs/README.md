# Conda environments


This directory contains Conda environment definition files used by each
module of the Snakemake workflow.

Each environment file specifies the software dependencies required for a
specific pipeline module, ensuring reproducibility and isolation of tools
across different analysis steps.

These environment files are versioned.