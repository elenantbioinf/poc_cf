# Results


This directory contains the results generated by the pipeline.

Results will be organized into subdirectories for each pipeline module.
Once the analysis is complete, there should be one subdirectory per step.

These results and subdirectories are not versioned.