# Reports directory

This directory contains the source templates used to generate web-based reports.

Files in this directory are versioned because they are part of the project logic.

The RMarkdown templates are rendered by Snakemake to produce the final HTML reports in the `final_report/` directory.

Outputs generated from these templates are not versioned.