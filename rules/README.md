# Rules


This directory contains the Snakemake rule files that define the workflow logic
of the pipeline.

Each file corresponds to a specific analysis module and describes the inputs,
outputs, parameters, environments and execution steps for that stage of the workflow.

The rules orchestrate the execution of scripts and tools in a reproducible and
modular manner.

These files are versioned.