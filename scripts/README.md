# Scripts


This directory contains auxiliary scripts used by the Snakemake workflow.

Each script implements a specific step or tool and is executed from the 
corresponding Snakemake rule. 

Scripts are written to be modular and reusable. 

They are not intended to be run manually outside the workflow context. 

These files are versioned.